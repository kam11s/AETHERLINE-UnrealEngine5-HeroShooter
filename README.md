# RAVELIN
Internal codename: Aetherline. Unreal Engine 5.8 C++ first-person hero shooter.

Do not rename the `Aetherline` module or `AL*` classes.

Agents: read `AGENTS.md` and `COMPILE_RULES.txt` before editing.

Play: open `Aetherline.uproject` in UE 5.8 → Yes on rebuild → Play.
